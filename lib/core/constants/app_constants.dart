/// Shared application-level constants used across multiple files.
class AppConstants {
  AppConstants._();

  // SharedPreferences keys
  static const String onboardingCompletedKey = 'onboarding_completed';
}
